context.objects = [
	{
		factory = adapter
		args = {
			factory.name	= support.null-audio-sink
			node.name   	= "to-ic-705"
			object.linger	= true
		}
	},
	{
		factory = adapter
		args = {
			factory.name	= support.null-audio-sink
			node.name   	= "from-ic-705"
			object.linger	= true
		}
	}
]
