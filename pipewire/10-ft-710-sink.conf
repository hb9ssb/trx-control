context.objects = [
	{
		factory = adapter
		args = {
			factory.name	= support.null-audio-sink
			node.name   	= "to-ft-710"
			object.linger	= true
		}
	},
	{
		factory = adapter
		args = {
			factory.name	= support.null-audio-sink
			node.name   	= "from-ft-710"
			object.linger	= true
		}
	}
]
